import { IfHasPermissionDirective } from './if-has-permission.directive';

describe('IfHasPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new IfHasPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
