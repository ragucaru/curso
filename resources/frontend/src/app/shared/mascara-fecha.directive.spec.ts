import { MascaraFechaDirective } from './mascara-fecha.directive';

describe('MascaraFechaDirective', () => {
  it('should create an instance', () => {
    const directive = new MascaraFechaDirective();
    expect(directive).toBeTruthy();
  });
});
